# Direção: <nome> (`<id>`)

- **Status:** Rascunho | Curada  ·  **Fonte destilada:** <design system / lib> · **Licença:** <MIT / Apache-2.0 / …>
- **Quando usar:** <o tipo de projeto/tela que pede esta direção>
- **Personalidade em uma frase:** <ex.: "instrumento de engenharia financeira: sóbrio e denso">

> Destilado de fonte SEM copyright: princípios e tokens, nunca markup copiado nem o "jeitão"
> de um produto real. Preencha os valores reais na curadoria. `[A DEFINIR]` = ainda não pesquisado.

## Tokens (viram `src/styles/tokens.css` se escolhida)

### Cor (oklch — acentos com mesmos chroma/lightness, variando o hue)
| Token | Valor | Uso |
|-------|-------|-----|
| `--bg-0`   | [A DEFINIR] | fundo da página |
| `--bg-1`   | [A DEFINIR] | painéis / cards |
| `--line`   | [A DEFINIR] | bordas / hairline |
| `--text-0` | [A DEFINIR] | texto primário |
| `--text-1` | [A DEFINIR] | texto secundário |
| `--accent` | [A DEFINIR] | acento da marca / interativo |
| `--pos`    | [A DEFINIR] | valor positivo (crédito) |
| `--neg`    | [A DEFINIR] | valor negativo (débito) |

### Tipografia (1-3 fontes; NUNCA Inter/Roboto/Arial no corpo; fallback de métricas próximas)
| Papel | Família | Fallback |
|-------|---------|----------|
| Display / títulos | [A DEFINIR] | [A DEFINIR] |
| Corpo / UI        | [A DEFINIR] | [A DEFINIR] |
| Números / ids (mono) | [A DEFINIR] | ui-monospace, monospace |

### Forma & ritmo
| Token | Valor |
|-------|-------|
| `--radius` | [A DEFINIR] |
| escala de `--space` | [A DEFINIR] (base 4/8) |
| densidade | compacta / confortável |

## Vocabulário de componente
<Como são botão, card, tabela, input e chip nesta direção — anatomia e estados, em prosa
curta. Extraído dos PRINCÍPIOS da fonte, nunca do markup dela.>

## O que a torna memorável (anti-genérico)
<O 1-2 traços de assinatura que fazem esta direção NÃO parecer template de IA.>
